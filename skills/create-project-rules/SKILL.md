---
name: "create-project-rules"
description: "Creates project-specific rules based on project information like architecture and programming language. Invoke when user asks to create project rules, 制定项目规则, or after project architecture and tech stack are defined."
---

# Create Project Rules（ECC 增强版）

## 技能描述

根据项目的基本信息（设计架构、使用的语言、技术栈等），自动制定适合该项目的规则文件。该技能会分析项目特征，生成包含代码规范、工作流程、质量标准等内容的规则文件。

**ECC 增强功能：**
- ✨ **交互式问答** - 通过友好的问答方式收集项目信息
- 📚 **模板复用** - 使用 ECC 现有的规则模板（coding-style, testing, git-workflow 等）
- 🎯 **语言特定** - 支持 JavaScript, TypeScript, Python, Go 等语言的最佳实践
- 🔄 **智能合并** - 将语言特定规范与 ECC 通用规则智能合并
- 📝 **即用即得** - 生成的规则文件直接保存到 `.claude/rules/project-rules.md`

## 触发条件

当用户出现以下情况时调用此技能：
- 用户说"创建项目规则"、"制定项目规则"、"生成项目规则"
- 用户说"根据项目信息制定规则"、"为项目生成规范"
- 项目架构和技术栈确定后，需要制定项目规范
- 项目初始化阶段，需要建立开发规范

## 输入要求

需要用户提供以下信息：
1. **项目类型**：如Web应用、移动应用、后端服务、桌面应用等
2. **编程语言**：如JavaScript、Python、Java、Go等
3. **技术栈**：如React、Vue、Spring Boot、Django等
4. **架构设计**：如单体应用、微服务、前后端分离等
5. **团队规模**：如个人项目、小团队、大团队
6. **特殊需求**：如性能要求、安全要求、合规要求等

## 执行步骤

### 1. 交互式收集项目信息（使用 AskUserQuestion）

使用友好的问答方式收集项目信息：

```python
# 问题 1: 项目类型
options = ["Web 应用", "移动应用", "后端服务", "桌面应用", "CLI 工具"]

# 问题 2: 编程语言
options = ["JavaScript", "TypeScript", "Python", "Go", "Java", "Rust", "Ruby", "PHP"]

# 问题 3: 技术栈（根据语言动态调整）
# TypeScript: ["React", "Vue", "Angular", "Next.js", "Express", "Nest.js"]
# Python: ["Django", "Flask", "FastAPI", "Pandas", "PyTorch"]
# Go: ["Gin", "Echo", "Fiber", "gRPC"]

# 问题 4: 架构设计
options = ["单体应用", "微服务", "前后端分离", "Serverless", "单页应用 (SPA)"]

# 问题 5: 团队规模
options = ["个人项目", "小团队 (2-5人)", "中型团队 (6-20人)", "大型团队 (20+人)"]
```

### 2. 构建项目信息 JSON

将收集的信息转换为 JSON 格式：

```json
{
  "name": "my-app",
  "type": "web-application",
  "language": "typescript",
  "tech_stack": ["react", "next.js", "tailwindcss"],
  "architecture": "frontend-backend-separation",
  "team_size": "small-team"
}
```

### 3. 调用 generate-rules.py 生成规则

```bash
cd skills/create-project-rules
python scripts/generate-rules.py '{
  "name": "my-app",
  "language": "typescript",
  "type": "web-application",
  "tech_stack": ["react", "next.js"],
  "architecture": "frontend-backend-separation",
  "team_size": "small-team"
}'
```

### 4. 规则生成逻辑

脚本执行以下步骤：

1. **加载语言模板**：从 `templates/{language}.md` 加载语言特定规范
2. **加载 ECC 规则**：从 `.claude/rules/` 加载现有的通用规则
3. **生成规则头部**：包含项目信息、版本、创建时间
4. **合并规则内容**：
   - 第一部分：语言特定规范（来自模板）
   - 第二部分：通用规则（继承自 ECC）
5. **保存规则文件**：输出到 `.claude/rules/project-rules.md`

### 5. 验证生成的规则

检查生成的规则文件：
- 是否包含语言特定规范
- 是否包含 ECC 通用规则
- 格式是否正确
- 内容是否完整

## 输出格式

生成一个完整的Markdown格式规则文件，包含以下结构：

```markdown
# 项目规则

## 版本信息
- 当前版本
- 最后更新
- 维护者
- 变更日志

## 基础配置
- 语言
- 系统
- 代码注释规范
- 包管理器

## 代码规范
- 命名规范
- 代码风格
- 最佳实践
- 质量要求

## 工作流程
- 开发流程
- 测试流程
- 部署流程

## 质量标准
- 代码复杂度
- 测试覆盖率
- 性能指标

## 安全规范
- 安全边界
- 敏感信息处理
- 权限管理

## 团队协作
- 代码审查
- 分支策略
- 沟通机制
```

## 约束条件

- 不修改现有代码，只生成规则文件
- 规则内容基于项目实际需求，避免过度规范
- 保持规则的可执行性和可维护性
- 提供规则的优先级和适用场景

## 使用示例

### 完整工作流示例

**用户请求：**
```
请为我的项目创建规则
```

**Claude 响应：**

1. **收集项目信息**（使用 AskUserQuestion）

```
问题 1: 项目类型是什么？
□ Web 应用
□ 移动应用
□ 后端服务
□ 桌面应用
□ CLI 工具

用户选择：Web 应用
```

```
问题 2: 使用什么编程语言？
□ JavaScript
□ TypeScript ✓
□ Python
□ Go
□ Java

用户选择：TypeScript
```

```
问题 3: 使用什么技术栈？（可多选）
□ React ✓
□ Vue
□ Next.js ✓
□ Express
□ Nest.js

用户选择：React, Next.js
```

```
问题 4: 架构设计是什么？
□ 单体应用
□ 微服务
□ 前后端分离 ✓
□ Serverless

用户选择：前后端分离
```

```
问题 5: 团队规模？
□ 个人项目
□ 小团队 (2-5人) ✓
□ 中型团队 (6-20人)
□ 大型团队 (20+人)

用户选择：小团队
```

2. **生成规则文件**

```bash
# Claude 执行
python skills/create-project-rules/scripts/generate-rules.py '{
  "name": "my-app",
  "language": "typescript",
  "type": "web-application",
  "tech_stack": ["react", "next.js"],
  "architecture": "frontend-backend-separation",
  "team_size": "small-team"
}'
```

3. **输出结果**

```
✅ 项目规则已生成：.claude/rules/project-rules.md

项目信息：
  - 名称：my-app
  - 语言：TypeScript
  - 类型：Web 应用
  - 技术栈：React, Next.js
  - 架构：前后端分离
  - 团队规模：小团队

规则文件包含：
  ✓ TypeScript 特定规范（类型定义、命名规范、函数式编程）
  ✓ ECC 通用规则（coding-style, testing, git-workflow, security）
  ✓ 工具配置（ESLint, Prettier, tsconfig.json）
```

### 生成的规则文件结构

```markdown
# my-app 项目规则

## 版本信息
- 版本：1.0.0
- 创建时间：2026-02-23
- 项目类型：Web 应用
- 编程语言：TypeScript
- 技术栈：React, Next.js
- 架构设计：前后端分离
- 团队规模：小团队

## 规则说明
本规则文件由 Everything Claude Code 的 `create-project-rules` 技能自动生成。

规则分为两部分：
1. **语言特定规范** - 针对 TypeScript 的最佳实践
2. **通用规则** - 继承自 ECC 的核心规则

---

## 语言特定规范

### TypeScript 配置
...（来自 templates/typescript.md）

### 类型定义
...

---

## 通用规则（继承自 ECC）

以下规则继承自 Everything Claude Code 的核心规则，适用于所有项目。

### 代码规范
...（来自 .claude/rules/coding-style.md）

### 测试要求
...（来自 .claude/rules/testing.md）

### Git 工作流
...（来自 .claude/rules/git-workflow.md）

### 安全指南
...（来自 .claude/rules/security.md）
```

## 与 ECC 命令的集成

生成的项目规则会自动被 ECC 的其他命令使用：

### 1. 代码审查（/code-review）

```bash
# code-reviewer 智能体会参考项目规则
/code-review
```

审查时会检查：
- 是否符合语言特定规范（如 TypeScript 类型定义）
- 是否遵循 ECC 通用规则（如不可变性、错误处理）
- 是否满足项目特定要求（如技术栈最佳实践）

### 2. 安全审查（/security-review）

```bash
# security-reviewer 智能体会参考安全规则
/security-review
```

审查时会检查：
- 项目规则中的安全规范
- ECC 通用安全指南
- 语言特定的安全最佳实践

### 3. 测试驱动开发（/tdd）

```bash
# tdd-guide 智能体会参考测试规则
/tdd
```

开发时会遵循：
- 项目规则中的测试要求（80% 覆盖率）
- ECC 的 TDD 工作流（RED → GREEN → REFACTOR）
- 语言特定的测试模式

### 4. 规则维护

当项目需求变化时，可以重新运行此技能更新规则：

```bash
# 重新生成规则（会覆盖现有规则）
Skill("create-project-rules")
```

或手动编辑规则文件：

```bash
# 编辑项目规则
vim .claude/rules/project-rules.md
```

## 支持的语言和模板

当前支持以下语言的特定规范：

| 语言 | 模板文件 | 包含内容 |
|------|---------|---------|
| JavaScript | `templates/javascript.md` | ES2020+、箭头函数、解构、展开运算符、async/await、数组方法、模块导入导出 |
| TypeScript | `templates/typescript.md` | tsconfig.json、类型定义、命名规范、函数式编程、异步处理、导入导出 |
| Python | `templates/python.md` | PEP 8、类型提示、dataclasses、错误处理、上下文管理器、列表推导式、装饰器 |
| Go | 待添加 | - |
| Java | 待添加 | - |
| Rust | 待添加 | - |

**扩展模板：**

如需添加新语言支持，在 `templates/` 目录创建 `{language}.md` 文件：

```bash
# 创建 Go 模板
touch skills/create-project-rules/templates/go.md
```

模板格式参考现有的 `typescript.md` 或 `python.md`。

## 注意事项

1. **项目适配性**：规则必须符合项目实际情况，避免照搬通用模板
2. **可执行性**：规则内容必须具体可执行，避免抽象描述
3. **灵活性**：为不同规模和复杂度的项目提供灵活的规则选项
4. **可维护性**：规则文件结构清晰，便于后续更新和维护
5. **团队共识**：规则制定后需要团队达成共识并遵守

## 技术实现

### 核心脚本

`scripts/generate-rules.py` - 主生成脚本

**关键类：**
```python
class ProjectRulesGenerator:
    def load_language_template(self, language: str) -> str
    def load_ecc_rules(self) -> Dict[str, str]
    def generate_header(self, project_info: dict) -> str
    def generate_language_section(self, language: str) -> str
    def generate_ecc_rules_section(self, ecc_rules: Dict[str, str]) -> str
    def generate(self, project_info: dict) -> str
    def save(self, content: str, output_path: Optional[Path] = None) -> Path
```

### 配置文件

`config.json` - 技能配置

```json
{
  "version": "1.0.0",
  "supported_languages": ["javascript", "typescript", "python", "go", "java", "rust", "ruby", "php"],
  "rule_sections": ["coding-style", "testing", "git-workflow", "security", "performance", "patterns", "hooks", "agents"],
  "templates_dir": "templates/",
  "ecc_rules_dir": ".claude/rules/",
  "output_dir": ".claude/rules/"
}
```

### 模板目录

`templates/` - 语言特定模板

- `javascript.md` - JavaScript ES2020+ 规范
- `typescript.md` - TypeScript 规范
- `python.md` - Python PEP 8 规范
- 更多语言模板待添加...

## 故障排除

### 问题 1: 生成的规则文件为空

**原因：** 语言模板不存在或 ECC 规则目录不存在

**解决：**
```bash
# 检查模板是否存在
ls skills/create-project-rules/templates/

# 检查 ECC 规则是否存在
ls .claude/rules/
```

### 问题 2: Python 脚本执行失败

**原因：** Python 版本不兼容或缺少依赖

**解决：**
```bash
# 检查 Python 版本（需要 3.7+）
python --version

# 脚本仅使用标准库，无需额外依赖
```

### 问题 3: 规则文件格式错误

**原因：** 模板文件格式不正确

**解决：**
```bash
# 参考现有模板格式
cat skills/create-project-rules/templates/typescript.md
```

## 未来扩展

### 短期（1-2 周）

- [ ] 添加 Go 语言模板
- [ ] 添加 Java 语言模板
- [ ] 添加 Rust 语言模板
- [ ] 支持自定义模板路径

### 中期（1-2 个月）

- [ ] 支持规则版本管理（历史记录）
- [ ] 支持规则差异对比
- [ ] 支持规则导入/导出
- [ ] 支持团队规则共享

### 长期（3-6 个月）

- [ ] 集成 continuous-learning v2（从会话中学习规则）
- [ ] 支持规则置信度评分
- [ ] 支持规则自动演进
- [ ] 支持跨项目规则复用
