#!/bin/bash
# Claudeception 自动激活钩子
#
# 在每个用户提示时注入提醒，让 Claude 评估是否有可提取的知识。
#
# 安装方式：
#   用户级：复制到 ~/.claude/hooks/ 并在 ~/.claude/settings.json 中配置
#   项目级：复制到 .claude/hooks/ 并在 .claude/settings.json 中配置
#
# 钩子配置（~/.claude/settings.json）：
# {
#   "hooks": {
#     "UserPromptSubmit": [{
#       "hooks": [{
#         "type": "command",
#         "command": "~/.claude/hooks/claudeception-activator.sh"
#       }]
#     }]
#   }
# }

cat << 'EOF'
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧠 技能提取评估提醒
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

完成用户请求后，评估是否产生了可提取的知识：

1. 先完成用户的请求
2. 自问：
   - 是否涉及非显而易见的调查或调试？
   - 解决方案是否对未来类似情况有帮助？
   - 是否发现了文档中不明显的东西？

3. 如果任一回答为"是"：
   使用 Skill(claudeception) 提取知识

4. 如果全部为"否"：
   跳过，无需提取

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EOF
